// Placeholder starter for AIModule rewrite.
"use client";

export default function InfoRow() {
  return <div>Full rewrite must be generated across multiple responses due to model output limits.</div>;
}
